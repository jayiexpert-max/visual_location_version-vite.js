# Abdul AI reads DB + AI settings from the project root .env (../../.env).
# Copy this block into visual_inventory/.env if not already present.

AI_PROVIDER=ollama
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=gemma3:4b

# AI_PROVIDER=gemini
# GEMINI_API_KEY=your_key_here
# GEMINI_MODEL=gemini-1.5-flash
